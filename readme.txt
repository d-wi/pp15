*** How To ***

1) start Vagrant using “vagrant up”

2) connect via “vagrant ssh”

3) compile the program 

	3.1) OpenMP example: “gcc -o openmp -fopenmp openmp_test.c

4) start the program (e.g. “./openmp_test” )